name: DoseCare Android Release Check

on:
  workflow_dispatch:
  push:
    branches: [dosecare-v2-production-hardening]
    paths:
      - 'dosecare-v2/**'
      - 'package.json'
      - 'capacitor.config.json'
      - '.github/workflows/mobile-android-release-check.yml'

jobs:
  android-release-check:
    name: Validate Android release AAB + APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Capacitor dependencies
        run: npm install

      - name: Add Android platform
        run: npx cap add android

      - name: Sync web assets into Android project
        run: npx cap sync android

      - name: Verify Android SDK configuration
        working-directory: android
        run: |
          echo "Checking generated Capacitor Android configuration..."
          grep -R "targetSdkVersion\|compileSdkVersion" -n variables.gradle app/build.gradle

      - name: Build release AAB
        working-directory: android
        run: ./gradlew bundleRelease --no-daemon

      - name: Build release APK
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon

      - name: Show Android release outputs
        run: |
          echo "AAB outputs:"
          find android/app/build/outputs/bundle -type f -maxdepth 3 -print
          echo "APK outputs:"
          find android/app/build/outputs/apk -type f -maxdepth 3 -print

      - name: Upload release AAB
        uses: actions/upload-artifact@v4
        with:
          name: dosecare-release-aab-check
          path: android/app/build/outputs/bundle/release/*.aab
          if-no-files-found: error

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: dosecare-release-apk-v2
          path: android/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
