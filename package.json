{
  "name": "dosecare",
  "version": "1.0.0",
  "private": true,
  "description": "DoseCare pediatric oral-liquid dose calculator mobile shell",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
