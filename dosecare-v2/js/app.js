/* DoseCare V2 — shared navigation helpers. */
(function(){
'use strict';
const welcome=document.getElementById('start-calculator');
if(welcome) welcome.addEventListener('click',()=>{window.location.href='calculator.html';});
})();
