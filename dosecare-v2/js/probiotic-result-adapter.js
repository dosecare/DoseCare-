/* DoseCare V2 — probiotic product result renderer. */
(function(){
  'use strict';
  const root=document.getElementById('result-stack');
  if(!root||!location.hash.startsWith('#data='))return;
  let raw;try{raw=decodeURIComponent(location.hash.slice(6));}catch{return;}
  let d;try{d=JSON.parse(raw);}catch{return;}
  if(!d?.ok||!['probiotic_fixed','probiotic_product'].includes(d?.calculationType)||d?.medicine?.id!=='probiotics')return;
  const m=d.medicine,r=d.regimen,info=m.information||{};
  const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const fmt=v=>{const n=Number(v);if(!Number.isFinite(n))return '—';if(n>=1e9){const b=n/1e9;return `${Number.isInteger(b)?b:b.toFixed(1)} billion`;};if(n>=1e6){const x=n/1e6;return `${Number.isInteger(x)?x:x.toFixed(1)} million`;};if(n>=1e3){const x=n/1e3;return `${Number.isInteger(x)?x:x.toFixed(1)} thousand`;};return String(n).replace(/\.0+$/,'');};
  const administration=d.calculationType==='probiotic_product' ? (d.administrationMin===d.administrationMax ? `${d.administrationMin} ${d.administrationUnit||'packet/day'}` : `${d.administrationMin}–${d.administrationMax} ${d.administrationUnit||'packet/day'}`) : null;
  const targetDose=d.calculationType==='probiotic_product' ? (d.lowProbiotic===d.highProbiotic ? `${fmt(d.lowProbiotic)} ${d.doseUnit||''}` : `${fmt(d.lowProbiotic)}–${fmt(d.highProbiotic)} ${d.doseUnit||''}`) : (d.lowProbiotic===d.highProbiotic ? `${fmt(d.lowProbiotic)} ${d.doseUnit||''}` : `${fmt(d.lowProbiotic)}–${fmt(d.highProbiotic)} ${d.doseUnit||''}`);
  const duration=r.durationDays!=null&&r.durationDays!=='' ? `${r.durationDays} days` : 'Not specified by the selected guideline';
  const refs=(m.sources||info.sources||[]).map(x=>`<a class="reference-item" href="${esc(x.url)}" target="_blank" rel="noopener noreferrer"><span>${esc(x.organization||'Source')}</span><strong>${esc(x.title||'Reference')}</strong></a>`).join('');
  const doseLine=administration ? `Take ${administration}` : targetDose;
  root.innerHTML=`<article class="glass-card result-card"><p class="eyebrow">PATIENT INFORMATION</p><div class="info-grid"><div><span>Medicine</span><strong>${esc(m.name)}</strong></div><div><span>Condition / regimen</span><strong>${esc(r.condition)}</strong></div><div><span>Age</span><strong>${esc(String(d.age??'')||'Not recorded')}</strong></div><div><span>Weight</span><strong>Not required</strong></div><div><span>Preparation</span><strong>${esc(d.formulation?.display||'Product-specific oral probiotic')}</strong></div><div><span>Duration</span><strong>${esc(duration)}</strong></div></div><div class="dose-output"><strong>${esc(doseLine)}</strong></div></article><article class="glass-card result-card"><p class="eyebrow">STEP-BY-STEP CALCULATION</p><div class="calculation-list"><div class="calc-step"><span>01</span><div><strong>Product strength</strong><p>${esc(d.formulation?.display||'Selected product')}</p></div></div><div class="calc-step"><span>02</span><div><strong>Daily administration</strong><p><b>${esc(doseLine)}</b></p></div></div><div class="calc-step"><span>03</span><div><strong>CFU target</strong><p>${esc(targetDose)}</p></div></div><div class="calc-step"><span>04</span><div><strong>Duration</strong><p>${esc(duration)}${r.frequencyText?` — ${esc(r.frequencyText)}`:''}</p></div></div><div class="calc-step"><span>05</span><div><strong>No CFU → mL conversion</strong><p>The selected product is packet-based, so DoseCare gives the labeled packet administration rather than inventing a liquid concentration.</p></div></div></div></article><article class="glass-card result-card"><p class="eyebrow">ADDITIONAL DETAILS</p><h1 class="medicine-heading">${esc(m.name)}</h1><div class="info-grid"><div><span>Active ingredient</span><strong>${esc(m.activeIngredient)}</strong></div><div><span>Drug class</span><strong>${esc(info.class)}</strong></div><div><span>Product strength</span><strong>${esc(d.formulation?.sachetContent?.lggCfu?`${fmt(d.formulation.sachetContent.lggCfu)} CFU per packet`:'See selected product')}</strong></div><div><span>Administration</span><strong>${esc(doseLine)}</strong></div></div><div class="info-block"><span>Mechanism</span><p>${esc(info.mechanism||'Not configured')}</p></div><div class="info-block"><span>Indications</span><p>${esc((info.indications||[]).join(' • '))}</p></div><div class="info-block"><span>Precautions & warnings</span><p>${esc((info.precautions||[]).join(' • '))}</p></div><div class="info-block"><span>Adverse effects</span><p>${esc((info.adverseEffects||[]).join(' • '))}</p></div><div class="info-block"><span>Administration instructions</span><p>${esc(info.administration||'Follow product instructions.')}</p></div><div class="sources"><p class="eyebrow">SOURCES & REFERENCES</p>${refs||'<p>No source record attached.</p>'}</div></article>`;
})();
